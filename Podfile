# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'dotogether' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  
  pod 'Then'
  pod 'SnapKit', '~> 5.0.0'
  pod 'Alamofire', '4.7.3'

  # Pods for dotogether

  target 'dotogetherTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
